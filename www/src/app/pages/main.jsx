'use strict';let cookie = require('cookie-cutter');let React = require('react');let Router = require('react-router');let RouteHandler = Router.RouteHandler;let LeftNav = require('./components/left-nav.jsx');class Main extends React.Component {  render() {    let menuItems = [];    if(!cookie.get('user_id') || !cookie.get('auth_token')){      menuItems = [      { type: "line"},        { type: "link", route: 'packs', text: 'Find Packs'},        { type: "link", route: 'cart', text: 'Shopping Cart'},      ];    }else{       menuItems = [       { type: "line"},        { type: "link", route: 'my-pack', text: 'My Packs'},        { type: "link", route: 'packs', text: 'Find Packs'},        { type: "link", route: 'cart', text: 'Shopping Cart'},        { type: "line"},        { type: "link", route: 'setting', text: 'Account'},        { type: "link", route: 'logout', text: 'Log out'},      ];    }      return (        <div className="row">          <section className="col-md-2 col-sm-12 col-xs-12">            <LeftNav menuItems={menuItems} />          </section>          <section className="col-md-8 col-sm-12 col-xs-12 main">            <RouteHandler />          </section>        </div>      );  }}module.exports = Main;